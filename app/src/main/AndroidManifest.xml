<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.santalu.textmatcher.sample">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme"
      tools:ignore="GoogleAppIndexingWarning">

    <activity
        android:name=".MainActivity"
        android:screenOrientation="portrait">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
        android:name=".SingleRuleActivity"
        android:label="@string/title_single_rule"
        android:screenOrientation="portrait" />

    <activity
        android:name=".MultiRuleActivity"
        android:label="@string/title_multi_rule"
        android:screenOrientation="portrait" />

    <activity
        android:name=".CustomRuleActivity"
        android:label="@string/title_custom_rule"
        android:screenOrientation="portrait" />

    <activity
        android:name=".SimpleTextActivity"
        android:label="@string/title_simple_text"
        android:screenOrientation="portrait" />

  </application>

</manifest>